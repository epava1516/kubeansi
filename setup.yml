---
  - name: Test local
    hosts: localhost
    become: true
    gather_facts: true
    tasks:
      - name: config
        include_role:
         name: "{{ roles }}"
        with_items:
        - pxeboot
        loop_control:
          loop_var: roles
...
